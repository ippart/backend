<ul class="breadcrumbs">
    <li><a href="{{ url('common/home', '') }}"><i class="fa fa-home"></i></a></li>
    {% for item in breadcrumbs %}
        <li><a href="{{ url(item.route, item.path)|raw }}">{{ item.title|raw }}</a></li>
    {% endfor %}
</ul>
